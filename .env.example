# Base URL of the backend REST API (leave unset to use the relative "/api" + dev proxy)
VITE_API_URL=http://localhost:3001/api

# URL of the backend Socket.IO server
VITE_SOCKET_URL=http://localhost:3001
